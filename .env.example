PORT=3000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/pedidos
LOG_LEVEL=info
# Logs legíveis no terminal (dev). Em produção deixe false: JSON puro.
LOG_PRETTY=true
# Limite do corpo JSON (documentos chegam em base64)
BODY_LIMIT=10mb
